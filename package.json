{
  "scripts": {
    "build:watch": "(cd api && npm run build:watch &) && (cd webhooks && npm run build:watch)",
    "build": "cd api && npm run build --watch",
    "test": "jest --coverage"
  },
  "dependencies": {
    "axios": "^1.2.2",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "sha1": "^1.1.1"
  }
}
